server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing: qualsiasi path che non corrisponde a un file statico
    # viene rimandato a index.html, dove React Router gestisce il routing
    location / {
        try_files $uri $uri/ /index.html;
    }
}
